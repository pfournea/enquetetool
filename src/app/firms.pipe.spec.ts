/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { FirmsPipe } from './firms.pipe';

describe('Pipe: Firms', () => {
  it('create an instance', () => {
    let pipe = new FirmsPipe();
    expect(pipe).toBeTruthy();
  });
});
